meta {
  name: Ready
  type: http
  seq: 2
}

get {
  url: {{gateway_url}}/v1/health/ready
  body: none
  auth: none
}

headers {
  Accept-Language: en
}

tests {
  test("expected status", function() {
    expect([200]).to.include(res.getStatus());
  });
  test("no server error", function() {
    expect(res.getStatus()).to.be.below(500);
  });
}

docs {
  Readiness: checks Postgres and Redis. Public.
  
  gRPC method: /health.v1.HealthService/Ready
  Expected statuses: 200
}
