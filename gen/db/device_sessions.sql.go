// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: device_sessions.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countActiveDevicesByUser = `-- name: CountActiveDevicesByUser :one
SELECT count(*) FROM auth.device_sessions
WHERE user_id = $1 AND revoked_at IS NULL AND last_activity > $2
`

type CountActiveDevicesByUserParams struct {
	UserID       int64
	LastActivity pgtype.Timestamptz
}

func (q *Queries) CountActiveDevicesByUser(ctx context.Context, arg CountActiveDevicesByUserParams) (int64, error) {
	row := q.db.QueryRow(ctx, countActiveDevicesByUser, arg.UserID, arg.LastActivity)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createDeviceSession = `-- name: CreateDeviceSession :one
INSERT INTO auth.device_sessions (
  user_id, platform, manufacturer, model, device_id,
  name, ip_address, user_agent, last_activity
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, now())
RETURNING id, user_id, platform, manufacturer, model, version, sdk, brand, hardware, board, device, product, is_physical_device, name, identifier, device_id, last_activity, ip_address, user_agent, location, revoked_at, revoked_reason, created_at
`

type CreateDeviceSessionParams struct {
	UserID       int64
	Platform     string
	Manufacturer *string
	Model        *string
	DeviceID     string
	Name         *string
	IpAddress    *string
	UserAgent    *string
}

func (q *Queries) CreateDeviceSession(ctx context.Context, arg CreateDeviceSessionParams) (AuthDeviceSession, error) {
	row := q.db.QueryRow(ctx, createDeviceSession,
		arg.UserID,
		arg.Platform,
		arg.Manufacturer,
		arg.Model,
		arg.DeviceID,
		arg.Name,
		arg.IpAddress,
		arg.UserAgent,
	)
	var i AuthDeviceSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Platform,
		&i.Manufacturer,
		&i.Model,
		&i.Version,
		&i.Sdk,
		&i.Brand,
		&i.Hardware,
		&i.Board,
		&i.Device,
		&i.Product,
		&i.IsPhysicalDevice,
		&i.Name,
		&i.Identifier,
		&i.DeviceID,
		&i.LastActivity,
		&i.IpAddress,
		&i.UserAgent,
		&i.Location,
		&i.RevokedAt,
		&i.RevokedReason,
		&i.CreatedAt,
	)
	return i, err
}

const getActiveDeviceSessionByUserAndDevice = `-- name: GetActiveDeviceSessionByUserAndDevice :one
SELECT id, user_id, platform, manufacturer, model, version, sdk, brand, hardware, board, device, product, is_physical_device, name, identifier, device_id, last_activity, ip_address, user_agent, location, revoked_at, revoked_reason, created_at FROM auth.device_sessions
WHERE user_id = $1 AND device_id = $2 AND revoked_at IS NULL
`

type GetActiveDeviceSessionByUserAndDeviceParams struct {
	UserID   int64
	DeviceID string
}

func (q *Queries) GetActiveDeviceSessionByUserAndDevice(ctx context.Context, arg GetActiveDeviceSessionByUserAndDeviceParams) (AuthDeviceSession, error) {
	row := q.db.QueryRow(ctx, getActiveDeviceSessionByUserAndDevice, arg.UserID, arg.DeviceID)
	var i AuthDeviceSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Platform,
		&i.Manufacturer,
		&i.Model,
		&i.Version,
		&i.Sdk,
		&i.Brand,
		&i.Hardware,
		&i.Board,
		&i.Device,
		&i.Product,
		&i.IsPhysicalDevice,
		&i.Name,
		&i.Identifier,
		&i.DeviceID,
		&i.LastActivity,
		&i.IpAddress,
		&i.UserAgent,
		&i.Location,
		&i.RevokedAt,
		&i.RevokedReason,
		&i.CreatedAt,
	)
	return i, err
}

const getDeviceSessionByID = `-- name: GetDeviceSessionByID :one
SELECT id, user_id, platform, manufacturer, model, version, sdk, brand, hardware, board, device, product, is_physical_device, name, identifier, device_id, last_activity, ip_address, user_agent, location, revoked_at, revoked_reason, created_at FROM auth.device_sessions WHERE id = $1
`

func (q *Queries) GetDeviceSessionByID(ctx context.Context, id int64) (AuthDeviceSession, error) {
	row := q.db.QueryRow(ctx, getDeviceSessionByID, id)
	var i AuthDeviceSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Platform,
		&i.Manufacturer,
		&i.Model,
		&i.Version,
		&i.Sdk,
		&i.Brand,
		&i.Hardware,
		&i.Board,
		&i.Device,
		&i.Product,
		&i.IsPhysicalDevice,
		&i.Name,
		&i.Identifier,
		&i.DeviceID,
		&i.LastActivity,
		&i.IpAddress,
		&i.UserAgent,
		&i.Location,
		&i.RevokedAt,
		&i.RevokedReason,
		&i.CreatedAt,
	)
	return i, err
}

const listActiveDevicesByUser = `-- name: ListActiveDevicesByUser :many
SELECT id, user_id, platform, manufacturer, model, version, sdk, brand, hardware, board, device, product, is_physical_device, name, identifier, device_id, last_activity, ip_address, user_agent, location, revoked_at, revoked_reason, created_at FROM auth.device_sessions
WHERE user_id = $1 AND revoked_at IS NULL AND last_activity > $2
ORDER BY last_activity DESC
`

type ListActiveDevicesByUserParams struct {
	UserID       int64
	LastActivity pgtype.Timestamptz
}

func (q *Queries) ListActiveDevicesByUser(ctx context.Context, arg ListActiveDevicesByUserParams) ([]AuthDeviceSession, error) {
	rows, err := q.db.Query(ctx, listActiveDevicesByUser, arg.UserID, arg.LastActivity)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuthDeviceSession{}
	for rows.Next() {
		var i AuthDeviceSession
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Platform,
			&i.Manufacturer,
			&i.Model,
			&i.Version,
			&i.Sdk,
			&i.Brand,
			&i.Hardware,
			&i.Board,
			&i.Device,
			&i.Product,
			&i.IsPhysicalDevice,
			&i.Name,
			&i.Identifier,
			&i.DeviceID,
			&i.LastActivity,
			&i.IpAddress,
			&i.UserAgent,
			&i.Location,
			&i.RevokedAt,
			&i.RevokedReason,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeAllDeviceSessionsByUser = `-- name: RevokeAllDeviceSessionsByUser :exec
UPDATE auth.device_sessions
SET revoked_at = now(), revoked_reason = $2
WHERE user_id = $1 AND revoked_at IS NULL
`

type RevokeAllDeviceSessionsByUserParams struct {
	UserID        int64
	RevokedReason *string
}

func (q *Queries) RevokeAllDeviceSessionsByUser(ctx context.Context, arg RevokeAllDeviceSessionsByUserParams) error {
	_, err := q.db.Exec(ctx, revokeAllDeviceSessionsByUser, arg.UserID, arg.RevokedReason)
	return err
}

const revokeDeviceSession = `-- name: RevokeDeviceSession :one
UPDATE auth.device_sessions
SET revoked_at = now(), revoked_reason = $2
WHERE id = $1 AND revoked_at IS NULL
RETURNING id, user_id, platform, manufacturer, model, version, sdk, brand, hardware, board, device, product, is_physical_device, name, identifier, device_id, last_activity, ip_address, user_agent, location, revoked_at, revoked_reason, created_at
`

type RevokeDeviceSessionParams struct {
	ID            int64
	RevokedReason *string
}

func (q *Queries) RevokeDeviceSession(ctx context.Context, arg RevokeDeviceSessionParams) (AuthDeviceSession, error) {
	row := q.db.QueryRow(ctx, revokeDeviceSession, arg.ID, arg.RevokedReason)
	var i AuthDeviceSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Platform,
		&i.Manufacturer,
		&i.Model,
		&i.Version,
		&i.Sdk,
		&i.Brand,
		&i.Hardware,
		&i.Board,
		&i.Device,
		&i.Product,
		&i.IsPhysicalDevice,
		&i.Name,
		&i.Identifier,
		&i.DeviceID,
		&i.LastActivity,
		&i.IpAddress,
		&i.UserAgent,
		&i.Location,
		&i.RevokedAt,
		&i.RevokedReason,
		&i.CreatedAt,
	)
	return i, err
}

const updateDeviceSessionActivity = `-- name: UpdateDeviceSessionActivity :exec
UPDATE auth.device_sessions
SET last_activity = now()
WHERE id = $1 AND revoked_at IS NULL
`

func (q *Queries) UpdateDeviceSessionActivity(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, updateDeviceSessionActivity, id)
	return err
}
